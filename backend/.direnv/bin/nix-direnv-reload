#!/usr/bin/env bash
set -e
if [[ ! -d "/home/anton/Dokumente/GitHub/recommender/backend" ]]; then
  echo "Cannot find source directory; Did you move it?"
  echo "(Looking for "/home/anton/Dokumente/GitHub/recommender/backend")"
  echo 'Cannot force reload with this script - use "direnv reload" manually and then try again'
  exit 1
fi

# rebuild the cache forcefully
_nix_direnv_force_reload=1 direnv exec "/home/anton/Dokumente/GitHub/recommender/backend" true

# Update the mtime for .envrc.
# This will cause direnv to reload again - but without re-building.
touch "/home/anton/Dokumente/GitHub/recommender/backend/.envrc"

# Also update the timestamp of whatever profile_rc we have.
# This makes sure that we know we are up to date.
touch -r "/home/anton/Dokumente/GitHub/recommender/backend/.envrc" "/home/anton/Dokumente/GitHub/recommender/backend/.direnv"/*.rc
